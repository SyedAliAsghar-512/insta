import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myshopholic.app',
  appName: 'Shopholic',
  webDir: 'build',
  server: {
    url: "https://shopholic-1w1s.onrender.com"
  }
};

export default config;
